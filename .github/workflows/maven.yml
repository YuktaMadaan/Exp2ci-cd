name: Maven Build (9 Goals)

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Maven Version
        run: mvn -v

      - name: 1) validate
        run: mvn validate

      - name: 2) compile
        run: mvn compile

      - name: 3) test
        run: mvn test

      - name: 4) package
        run: mvn package

      - name: 5) verify
        run: mvn verify

      - name: 6) install
        run: mvn install

      - name: 7) site
        run: mvn site

      - name: 8) clean
        run: mvn clean

      - name: 9) clean install (final)
        run: mvn clean install
